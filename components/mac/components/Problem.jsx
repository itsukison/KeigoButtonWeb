import RiseGradient from './RiseGradient.jsx'

/**
 * All of this section's copy was cut on 2026-08-07 — the H2 ("コピーしない。
 * 貼り付けない。切り替えない。") and the three lines under it. What is left is
 * the glow, so the section is now a pause between the hero and §4 rather than an
 * argument. content.md §2 still holds the text if it is ever wanted back; the
 * .problem__line rules are still in global.css for the same reason.
 */
export default function Problem() {
  return (
    <section className="section problem">
      <RiseGradient />
    </section>
  )
}
